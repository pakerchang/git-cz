## A custom commit list template

deps on commitzen, git-cz

## Usage

If commitzen is already install in node_modules or global node_modules,
clone this repo into your project, then call command to use.

```zsh
git cz
```
